{
  "name": "health-tracker-backend",
  "version": "1.0.0",
  "description": "Health tracker backend with WHOOP integration and food logging",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "node -e \"require('./database').init().then(() => { console.log('Database initialized'); process.exit(0); }).catch(err => { console.error(err); process.exit(1); })\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "pg": "^8.11.3",
    "tesseract.js": "^5.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
